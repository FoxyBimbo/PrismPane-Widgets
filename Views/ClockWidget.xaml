<Window x:Class="EchoUI.Views.ClockWidget"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Clock" Width="220" Height="220"
        WindowStyle="None" AllowsTransparency="True"
        Background="Transparent" Topmost="False"
        ShowInTaskbar="False" ResizeMode="CanResize">
    <WindowChrome.WindowChrome>
        <WindowChrome ResizeBorderThickness="8" CaptionHeight="0" GlassFrameThickness="0" CornerRadius="0"/>
    </WindowChrome.WindowChrome>
    <Grid>
        <Border x:Name="RootBorder" Background="{DynamicResource WindowBackgroundSemiBrush}" CornerRadius="14" Margin="8"
                BorderBrush="{DynamicResource AccentBrush}" BorderThickness="1.5"
                MouseLeftButtonDown="RootBorder_MouseLeftButtonDown">
            <Border.Effect>
                <DropShadowEffect BlurRadius="16" ShadowDepth="2" Opacity="0.5" Color="Black"/>
            </Border.Effect>
            <Grid Margin="12">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <DockPanel Grid.Row="0" Margin="0,0,0,6">
                    <Button Content="✕" Click="BtnClose_Click" DockPanel.Dock="Right"
                            Background="Transparent" Foreground="{DynamicResource MutedForegroundBrush}" BorderBrush="Transparent"
                            FontSize="14" Padding="4,0" Cursor="Hand" MinWidth="24" ToolTip="Close"/>
                    <Button Content="⚙" Click="BtnSettings_Click" DockPanel.Dock="Right"
                            Background="Transparent" Foreground="{DynamicResource MutedForegroundBrush}" BorderBrush="Transparent"
                            FontSize="14" Padding="4,0" Cursor="Hand" MinWidth="24" ToolTip="Settings"/>
                    <TextBlock Text="Clock" FontSize="16" FontWeight="Bold" Foreground="{DynamicResource ForegroundBrush}"/>
                </DockPanel>

                <Grid Grid.Row="1">
                    <Grid x:Name="DigitalPanel" Visibility="Collapsed">
                        <Viewbox Stretch="Uniform" StretchDirection="Both" Margin="4">
                            <StackPanel Width="240" Height="90" VerticalAlignment="Center" HorizontalAlignment="Center">
                                <TextBlock x:Name="TxtDigitalClock" FontSize="36" FontWeight="SemiBold" FontFamily="Consolas" TextAlignment="Center">
                                    <Run x:Name="RunHour" Text="00"/>
                                    <Run Text=":" Foreground="{DynamicResource ForegroundBrush}"/>
                                    <Run x:Name="RunMinute" Text="00"/>
                                    <Run Text=":" Foreground="{DynamicResource ForegroundBrush}"/>
                                    <Run x:Name="RunSecond" Text="00"/>
                                </TextBlock>
                                <TextBlock x:Name="TxtDigitalDate" Margin="0,8,0,0" FontSize="13" Foreground="{DynamicResource MutedForegroundBrush}" TextAlignment="Center"/>
                            </StackPanel>
                        </Viewbox>
                    </Grid>

                    <Grid x:Name="AnalogPanel" Visibility="Collapsed">
                        <Viewbox Stretch="Uniform" StretchDirection="Both" Margin="4">
                            <Canvas Width="180" Height="180" HorizontalAlignment="Center" VerticalAlignment="Center">
                                <Ellipse Width="180" Height="180" Stroke="{DynamicResource BorderBrush}" StrokeThickness="2" Fill="{DynamicResource ControlBackgroundBrush}"/>
                                <Line x:Name="HourHand" X1="90" Y1="90" X2="90" Y2="45" StrokeThickness="5" StrokeStartLineCap="Round" StrokeEndLineCap="Round">
                                    <Line.RenderTransform>
                                        <RotateTransform x:Name="HourRotate" CenterX="90" CenterY="90"/>
                                    </Line.RenderTransform>
                                </Line>
                                <Line x:Name="MinuteHand" X1="90" Y1="90" X2="90" Y2="30" StrokeThickness="3" StrokeStartLineCap="Round" StrokeEndLineCap="Round">
                                    <Line.RenderTransform>
                                        <RotateTransform x:Name="MinuteRotate" CenterX="90" CenterY="90"/>
                                    </Line.RenderTransform>
                                </Line>
                                <Line x:Name="SecondHand" X1="90" Y1="96" X2="90" Y2="20" StrokeThickness="2" StrokeStartLineCap="Round" StrokeEndLineCap="Round">
                                    <Line.RenderTransform>
                                        <RotateTransform x:Name="SecondRotate" CenterX="90" CenterY="90"/>
                                    </Line.RenderTransform>
                                </Line>
                                <Ellipse Width="8" Height="8" Fill="{DynamicResource ForegroundBrush}" Canvas.Left="86" Canvas.Top="86"/>
                            </Canvas>
                        </Viewbox>
                    </Grid>
                </Grid>
            </Grid>
        </Border>
    </Grid>
</Window>
